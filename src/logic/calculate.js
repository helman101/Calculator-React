const calculate = ({total, next, operation}, buttonName) => {
  switch(buttonName) {
    case '+/-':
        total *= -1
        next *= -1
      break;
    case y:
      // code block
      break;
  }
}
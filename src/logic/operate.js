import Big from 'big.js';

const operate = (numberOne, numberTwo, operation) => {
  switch(operation) {
    case '-':
        Big(numberOne) - Big(numberTwo)
      break;
    case y:
      // code block
      break;
  }
}